# خطة الترقية والتطوير — نَسَب (Nasab)

تاريخ التنفيذ: 2026-07-28  
المستودع: [github.com/ainoamn/Nasab](https://github.com/ainoamn/Nasab)  
الموقع: [nasab-mu.vercel.app](https://nasab-mu.vercel.app)

---

## الهدف

ربط الإنتاج بقاعدة **Neon PostgreSQL** الحقيقية، تثبيت الدخول، ورفع جاهزية الإطلاق.

---

## المرحلة 1 — قاعدة البيانات الحقيقية (Neon) ✅ منفَّذة محلياً

| الفحص | الحالة |
|--------|--------|
| اتصال Neon pooled (eu-west-2) | ✅ |
| 18 جدولاً في `public` | ✅ |
| مشرف `admin@bhd.om` / `password:admin@bhd.om` | ✅ |
| خطط الاشتراك (3) | ✅ بعد `npm run db:seed-neon` |
| بوابات الدفع (5) | ✅ |
| `npm run db:verify-neon` | ✅ كلها ناجحة |

أوامر التحقق:

```bash
cd app
npm run db:verify-neon
npm run admin:ensure
npm run db:seed-neon
```

---

## المرحلة 2 — ربط Vercel بالقاعدة ⚠️ يحتاج إجراء منك

`/api/diag` على الإنتاج يظهر حالياً:

```json
{ "dbConfigured": false, "hasAppSecret": false, "sidecar": true }
```

يعني الكود جاهز (sidecar موجود) لكن **متغيرات البيئة غير مضبوطة على Vercel**.

### نفّذ الآن (دقيقتان)

1. سجّل دخول CLI:
   ```bash
   cd app
   vercel login
   vercel link   # Root Directory = app
   npm run vercel:env
   ```
2. أو من لوحة Vercel → Settings → Environment Variables (Production):

| المتغير | القيمة |
|---------|--------|
| `DATABASE_URL` | رابط Neon **pooled** من `.env.production` |
| `APP_SECRET` | ≥ 32 حرفاً (يُولَّد تلقائياً عبر `vercel:env`) |
| `PASSWORD_LOGIN_EMAIL` | `admin@bhd.om` |
| `PASSWORD_LOGIN_PASSWORD` | كلمة قوية — أو `npm run admin:rotate` |
| `OWNER_UNION_ID` | `password:admin@bhd.om` |
| `APP_PUBLIC_URL` | `https://nasab-mu.vercel.app` |
| `ALLOWED_ORIGINS` | `https://nasab-mu.vercel.app` |
| `TRUST_PROXY` | `true` |

3. **Redeploy** للإنتاج.
4. تحقق: `https://nasab-mu.vercel.app/api/diag` → `dbConfigured: true`
5. دخول: `/login` → البريد من `PASSWORD_LOGIN_EMAIL` (بعد التدوير استخدم كلمة المرور الجديدة)

---

## المرحلة 3 — ترقيات المنتج (منفَّذة في هذا الإطلاق)

1. دخول عبر `POST /api/auth/password-login` (Hono) بدل tRPC input الذي يعلّق على Vercel.
2. Sidecar `db-pg.cjs` (Neon HTTP) يُحمَّل فقط عند الحاجة.
3. `/api/diag` لفحص الربط بدون كشف أسرار.
4. تنبيه في صفحة `/login` إذا القاعدة غير مربوطة.
5. سكربتات: `db:verify-neon`، `db:seed-neon`، `vercel:env`.

---

## المرحلة 4 — ترقية مستمرة (جارية)

| الأولوية | البند | الحالة |
|----------|--------|--------|
| عالية | تفعيل `DATABASE_URL` على Vercel | ⚠️ يدوي — `npm run vercel:print-env` ثم الصق في اللوحة |
| عالية | إصلاح قراءة جسم POST على Vercel | ✅ `shouldAddHelpers: false` + form-urlencoded |
| متوسطة | فحص دخان للإنتاج | ✅ `npm run prod:smoke` |
| متوسطة | نسخ احتياطي Neon مجدول | ✅ `Ops` workflow + سر `DATABASE_URL` |
| منخفضة | نطاق مخصص + بريد معاملاتي | لاحقاً |

### أوامر الإطلاق السريعة

```bash
cd app
npm run db:verify-neon
npm run vercel:print-env   # انسخ القيم إلى Vercel ثم Redeploy
npm run prod:smoke
```

---

## المرحلة 5 — واجهة الجاهزية + نسخ احتياطي (منفَّذة)

| البند | الحالة |
|--------|--------|
| صفحة `/setup` بفحص `/api/diag` | ✅ |
| رابط من `/login` عند غياب القاعدة | ✅ |
| `npm run db:backup-neon` | ✅ → `.data/backups/` |
| CI smoke اختياري على GitHub Actions | ✅ |

---

## المرحلة 6 — بصمة البناء + حالة الإطلاق (منفَّذة)

| البند | الحالة |
|--------|--------|
| `NASAB_BUILD_SHA` / `NASAB_BUILD_TIME` في دالة Vercel | ✅ |
| `/api/health` و`/api/diag` يعرضان `build` | ✅ |
| رابط جاهزية من تذييل الرئيسية | ✅ |
| `npm run launch:status` (Neon محلي + smoke حي) | ✅ |

---

## المرحلة 30 — تفاصيل/مسار/QR توأم + a11y مخطط + اختبارات (منفَّذة)

| البند | الحالة |
|--------|--------|
| روابط العائلة في بطاقة التفاصيل + مسار التركيز: TwinBadge | ✅ |
| ملخص العائلة للطباعة وQR: TwinBadge | ✅ |
| `common.emDash` في اللافتات والطباعة ونطاق الطباعة | ✅ |
| aria-label لأزرار التكبير والجولات والإخوة | ✅ |
| اختبارات spouseMeta / generationLimit / treeOccasions | ✅ |

---

## المرحلة 29 — منتقٍ/طباعة توأم + a11y إغلاق + زخارف CSS (منفَّذة)

| البند | الحالة |
|--------|--------|
| PersonSearchPicker + TwinFamilyPanel: TwinBadge | ✅ |
| ملف شخص وبطاقة مناسبة للطباعة: TwinBadge و`emDash` | ✅ |
| Dialog/Sheet `common.close` + AuthLayout `nav.navigation` | ✅ |
| عناوين Fan/مسار/أماكن/أقارب: `common.emDash` | ✅ |
| Ornate/Manuscript: زخارف CSS بدل ✦ | ✅ |
| اختبارات favoriteRelates / recentRelates / researchTour | ✅ |

---

## المرحلة 28 — مسارات/نواقص/جولات توأم + a11y + اختبارات عرض (منفَّذة)

| البند | الحالة |
|--------|--------|
| مسار البيت ومسار المشاركة: TwinBadge | ✅ |
| شريط النواقص + جولتا البحث والاتساق: TwinBadge و`emDash` | ✅ |
| Spinner / Sidebar: `common.loading` و`nav.toggle` | ✅ |
| وصف الفاتورة و`formatPalmCouple` يقبلان empty | ✅ |
| اختبارات `personDisplay` و`consistencyTourState` + حواف البحث | ✅ |

---

## المرحلة 27 — مناسبات/مشاركة/اكتشافات + شهادة قرابة + اختبارات تخزين (منفَّذة)

| البند | الحالة |
|--------|--------|
| لوحة المناسبات + حوار المشاركة: TwinBadge | ✅ |
| اكتشافات: TwinBadge + `emDash` | ✅ |
| شهادة القرابة: TwinBadge على الأسماء والخطوات | ✅ |
| شرطات فارغة في حساب/مشرف/فاتورة عبر i18n | ✅ |
| AuthLayout: تنقّل/دخول مترجم | ✅ |
| اختبارات recent/dismissed/occasionsScope/today | ✅ |

---

## المرحلة 26 — أماكن/صور/مناسبات توأم + مروحة + روابط (منفَّذة)

| البند | الحالة |
|--------|--------|
| أماكن / صور / شريط المناسبات / بانر اليوم: TwinBadge | ✅ |
| مروحة: عنوان إضافة أب/أم + تمييز توأم | ✅ |
| جيل صاعد وطباعة + قائمة: i18n (`generationUp` / `emDash`) | ✅ |
| بادئة مسار القرابة `pathHopPrefix` | ✅ |
| اختبارات `treeUrl` / `homePerson` / `favoritePeople` | ✅ |
| README: `prod:smoke` و`deploy:status` | ✅ |

---

## المرحلة 25 — تفاصيل/مسارات قرابة + طباعة جنس + 404 (منفَّذة)

| البند | الحالة |
|--------|--------|
| ورقة التفاصيل: TwinBadge + حرف الاسم بدل ♀♂ | ✅ |
| شريطا مقارنات القرابة (مفضلة/حديثة): TwinBadge | ✅ |
| بطاقة طباعة الشخص: جنس عبر i18n | ✅ |
| نسب/كبسولة: `common.emDash` | ✅ |
| دخول بلا نص عربي ثابت؛ 404 بتنبيه جاهزية | ✅ |
| اختبارات `buildOccasionIcs` و`personDisplayNameWithTwin` | ✅ |

---

## المرحلة 24 — مساحة العمل/طباعة/مشاركة + توأم في البحث والنسب (منفَّذة)

| البند | الحالة |
|--------|--------|
| مساحة العمل / الطباعة / المشاركة: تنبيه DB / تأخر البناء | ✅ |
| حديثاً / مفضلة / بحث المخطط: TwinBadge | ✅ |
| نسب الأسلاف والذرية (`MhPersonPill`): TwinBadge | ✅ |
| بوابة التحويل البنكي: Landmark بدل إيموجي | ✅ |
| مخطوط/زخرفة: `aria-hidden` على ✦ | ✅ |
| اختبارات اكتشاف `possibleTwin` | ✅ |
| `prod:smoke` يشترط بصمة بناء غير فارغة | ✅ |

---

## المرحلة 23 — جاهزية صفحات + توأم في القوائم + طباعة بلا إيموجي (منفَّذة)

| البند | الحالة |
|--------|--------|
| نجاح الدفع / الأعضاء / الدعوة: تنبيه DB / تأخر البناء | ✅ |
| قائمة الأشخاص + شريط العائلة: TwinBadge | ✅ |
| مناسبات الطباعة ومعايناتها: Lucide بدل إيموجي | ✅ |
| قبيلة/نخلة: ChevronDown وArrowLeft؛ افتراضي `twinMarkWord` | ✅ |
| ErrorBoundary ونصوص التهاني عبر i18n بلا إيموجي | ✅ |
| اختبارات `isFullSibling` / `computePersonRanks` | ✅ |

---

## المرحلة 22 — مسار قرابة توأم + إدارة/دفع + غلاف كتاب (منفَّذة)

| البند | الحالة |
|--------|--------|
| مسار القرابة: TwinBadge + فجوات توأم + شريط عائلة بـ people/rels | ✅ |
| `/admin` و`/checkout`: تنبيه DB / تأخر البناء | ✅ |
| غلاف الكتاب ومعاينة الكتاب بلا إيموجي | ✅ |
| معاينة النخلة بعلامة توأم | ✅ |
| عملة ر.ع./OMR + ErrorBoundary ثنائي اللغة | ✅ |

---

## المرحلة 21 — معاينات طباعة + حساب + كتاب + دخان أقوى (منفَّذة)

| البند | الحالة |
|--------|--------|
| شمس: فاصل الأسماء عبر `listJoin` | ✅ |
| معاينات القوالب: قبيلة/بطن/عائلة + علامات توأم | ✅ |
| `/account`: تنبيه DB / تأخر البناء | ✅ |
| كتاب الطباعة: سنوات الحياة + متوفى | ✅ |
| نموذج الشخص: بن/بنت عبر i18n | ✅ |
| `prod:smoke` يفشل إن تعطلت الصفحات/headers/رد الدخول | ✅ |

---

## المرحلة 20 — شمس/مشاركة/لوحة تحكم + i18n طباعة (منفَّذة)

| البند | الحالة |
|--------|--------|
| شمس: تسميات زوج/زوجة عبر i18n | ✅ |
| مشاركة: إجراءات فجوات التوأم (مسار/فتح) | ✅ |
| واتساب النواقص يذكر اسم التوأم المحتمل | ✅ |
| لوحة التحكم: تنبيه DB / تأخر البناء | ✅ |
| مخطوط/تراث/نخلة: basmala و nasabJoin و aria | ✅ |

---

## المرحلة 19 — توأم إنجليزي + فجوات توأم + ملاحظات زوجية (منفَّذة)

| البند | الحالة |
|--------|--------|
| `twinMarkWord` + تمرير حرف التوأم في قوالب الطباعة | ✅ EN=`T` / AR=`ت` |
| فجوات الشخص: ربط/مسار/فتح لتوأم محتمل | ✅ `otherPersonId` |
| سجل النسب: علامات توأم في القائمة | ✅ |
| ملاحظات زوج/زوجة عبر i18n في الطباعة والمخطط | ✅ |
| `launch:status`: Neon محلي استشاري فقط | ✅ |

---

## المرحلة 18 — اكتشاف توأم + طباعة i18n + Setup موحّد (منفَّذة)

| البند | الحالة |
|--------|--------|
| اكتشافات: ربط/إظهار/فتح لتوأم محتمل | ✅ |
| مروحة/شمس: `twins.mark` بدل حرف ت ثابت | ✅ |
| جذر نطاق الطباعة بعلامات توأم | ✅ |
| بطاقة شخص مطبوعة: توأم/متوفى عبر i18n | ✅ |
| `/setup` يستخدم `useBuildBehind` | ✅ |

---

## المرحلة 17 — ضيوف بقرابة + نخلة توأم + تنبيه الدخول (منفَّذة)

| البند | الحالة |
|--------|--------|
| قائمة ضيوف المناسبات: قرابة + تاريخ ميلاد | ✅ |
| سعف/فيضان/جذع النخلة بعلامات توأم | ✅ `formatPalmCouple(..., people)` |
| `/login` ينبّه عند تأخر البناء (`useBuildBehind`) | ✅ |
| اختبارات `birthOrder` + `relationshipLabel` | ✅ |
| `prod:smoke` يفحص `/` | ✅ |

---

## المرحلة 16 — قائمة ضيوف المناسبات + تنبيه الرئيسية (منفَّذة)

| البند | الحالة |
|--------|--------|
| قالب المناسبات: قائمة أقارب/ضيوف مرتبة بالميلاد + توأم | ✅ |
| تنبيه على `/` عند تأخر البناء عن GitHub `main` | ✅ |
| `prod:smoke` يعرض `liveBuild` / `originMain` / `deployInSync` | ✅ |

### إن بقي الحي على SHA قديم

نفس خطوات المرحلة 15: Vercel → Redeploy، Root Directory = `app`، ثم `npm run deploy:status` أو `npm run prod:smoke`.

---

## المرحلة 15 — كشف تأخر النشر + تراث (منفَّذة)

| البند | الحالة |
|--------|--------|
| `npm run deploy:status` (حي vs `origin/main`) | ✅ |
| تنبيه على `/setup` عند تأخر البناء | ✅ |
| شريط نسب التراث بعلامات توأم | ✅ |
| Ops workflow يفحص مزامنة SHA | ✅ |

### إن بقي الحي على SHA قديم

1. Vercel → Project → Settings → **Root Directory = `app`**
2. Deployments → … → **Redeploy** لآخر commit على `main`
3. تحقق: `npm run deploy:status` أو [/setup](https://nasab-mu.vercel.app/setup)

لا يوجد workflow ينشر إلى Vercel من GitHub — الاعتماد على تكامل Git في Vercel فقط.

---

## المرحلة 14 — قبيلة/ملصق + اختبار نخلة + بصمة CI (منفَّذة)

| البند | الحالة |
|--------|--------|
| علامات توأم في Clan / Poster (+ توحيد Map) | ✅ `personDisplayNameWithTwin` |
| اختبار ترتيب توائم النخلة | ✅ |
| `assert-build-fingerprint` في CI | ✅ |
| ملاحظة إن تأخر نشر Vercel | ✅ أدناه |

> إن بقي `/api/health` على SHA قديم بعد دفع `main`: من لوحة Vercel → Deployments → Redeploy (قد يتعطل النشر التلقائي).

---

## المرحلة 13 — مخطوط/خريطة + ترتيب توأم + أمان Ops (منفَّذة)

| البند | الحالة |
|--------|--------|
| شارات توأم في المخطوط والخريطة | ✅ |
| `PersonRankLines` يعرض تسمية التوأم | ✅ |
| جداول rate-limit / headers في DEPLOY | ✅ |
| `vercel:print-env --checklist` + فحص headers في smoke | ✅ |

---

## المرحلة 12 — نخلة عُمان + اكتشافات + Setup i18n (منفَّذة)

| البند | الحالة |
|--------|--------|
| ترتيب ميلاد + علامات توأم في قالب النخلة | ✅ |
| اكتشاف `possibleTwin` في Discoveries | ✅ |
| مفاتيح `setup.*` لصفحة الجاهزية | ✅ |
| دخان الإنتاج يفحص `/setup` | ✅ |

---

## المرحلة 11 — نواقص توأم + تدوير كلمة المرور (منفَّذة)

| البند | الحالة |
|--------|--------|
| فجوة `possibleTwin` للإخوة الأشقاء بنفس سنة الميلاد | ✅ |
| شارة توأم في قالب الكتاب | ✅ |
| `npm run admin:rotate` لتوليد/كتابة كلمة مرور الدخول | ✅ |

---

## المرحلة 10 — ترتيب مخطط مستقر + Ops مجدول (منفَّذة)

| البند | الحالة |
|--------|--------|
| `comparePeopleByBirth` في FamilyChart / print / Descendants | ✅ |
| شارة نوع التوأم في TwinFamilyPanel | ✅ |
| GitHub Actions يومي: `prod:smoke` + `db:backup-neon` | ✅ `.github/workflows/ops.yml` |
| توثيق النسخ والـ GEDCOM في DEPLOY | ✅ |

---

## المرحلة 9 — دورة توائم GEDCOM (منفَّذة)

| البند | الحالة |
|--------|--------|
| تصدير `_TGID` + `ASSO/RELA twin` | ✅ |
| استيراد ودمج مجموعات التوائم | ✅ |
| `importGedcom` يطبّق `twinGroupId` | ✅ |
| اختبارات round-trip | ✅ |

---

## المرحلة 8 — توائم وترتيب + صقل الإطلاق (منفَّذة)

| البند | الحالة |
|--------|--------|
| ترتيب ميلاد مستقر (`comparePeopleByBirth`) | ✅ |
| تمييز توأم مختلط + اختبارات | ✅ |
| شريط العائلة المباشرة يعرض تسمية التوأم | ✅ |
| نموذج دخول مع Enter + تعطيل عند غياب القاعدة | ✅ |
| `/api/diag` بدون كشف البريد — أعلام حضور فقط | ✅ |
| نسخ أوامر Vercel من `/setup` | ✅ |

---

## المرحلة 7 — بعد ربط القاعدة (مخطَّطة)

| الأولوية | البند | الحالة |
|----------|--------|--------|
| عالية | تدوير كلمة مرور المشرف بعد أول دخول ناجح | ✅ `npm run admin:rotate` (ثم حدّث Vercel) |
| عالية | إنشاء شجرة تجريبية والتحقق من الحفظ على الإنتاج | معلّق على Vercel DB |
| متوسطة | نسخ Neon مجدول | ✅ `ops.yml` |
| متوسطة | نطاق مخصص + شهادة HTTPS | لاحقاً |
| منخفضة | بريد معاملاتي للدعوات | لاحقاً |

---

## معيار نجاح الإطلاق

- [x] Neon يحتوي الجداول والمشرف والخطط
- [x] الكود مرفوع على `main` مع توثيق
- [x] بصمة البناء + `/setup` + `launch:status`
- [x] ترتيب توائم مستقر + صقل الدخول/التشخيص
- [x] دورة توائم GEDCOM (تصدير/استيراد)
- [x] ترتيب مخطط/طباعة مستقر + Ops مجدول
- [x] اقتراح توأم محتمل + تدوير كلمة مرور المشرف
- [x] نخلة عُمان + اكتشافات توأم + Setup i18n
- [x] مخطوط/خريطة + ترتيب توأم + توثيق الأمان
- [x] قبيلة/ملصق + اختبار نخلة + بصمة CI
- [x] كشف تأخر النشر (`deploy:status` + `/setup`)
- [ ] `/api/diag` على الإنتاج: `dbConfigured: true` ← **إجراء يدوي على Vercel**
- [ ] تسجيل دخول ناجح من `/login`
- [ ] إنشاء شجرة وحفظ شخص في الإنتاج
- [ ] إن تأخر البناء الحي: Redeploy يدوي من Vercel (Root Directory = `app`)
