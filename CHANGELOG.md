# سجل التغييرات — نَسَب (Nasab)

المستودع: [github.com/ainoamn/Nasab](https://github.com/ainoamn/Nasab)

---

## 2026-07-29 — إنتاج Neon + استيراد GEDCOM + دخول Google

### البنية والإنتاج
- إصلاح `$returningId` على Postgres: إدراج دفعي (`insertReturningIds` / `insertMany`) لتجنّب مهلة Vercel.
- إعدادات Vercel: Build = `node scripts/vercel-build.mjs`، Root = `app`، `maxDuration: 60`.
- استيراد GEDCOM يدمج مع الموجود (لا يكرّر) مع وضع استبدال اختياري؛ علامات `[[ged:KEY]]`.
- إعدادات الشجرة: إزالة المكررات + تفريغ الأفراد.

### المصادقة
- إلغاء Kimi من الواجهة.
- دخول المستخدمين عبر Google OAuth (تحويل `/api/oauth/google` + مسار ID-token احتياطي).
- دخول المشرف بالبريد تحت قسم مطوي؛ زر تسجيل خروج ظاهر في الهيدر.
- `/setup` يعرض حالة Google؛ CSP يسمح بـ accounts.google.com.

### توثيق
- [`UPGRADE.md`](./UPGRADE.md) المرحلة 33؛ تحديث [`DEPLOY.md`](./app/DEPLOY.md) و[`README.md`](./README.md).

---

## 2026-07-28 — توأم شمسي/مروحة + a11y مخطط + حدود أخطاء

### المنتج
- علامات توأم على أزواج المخطط الشمسي والمروحة وفلاتر الزوجات في المخطط العائلي.
- aria-label لشريط أدوات الشجرة ونقاط البحث وروابط الزوجية.
- رسالة خطأ ودّية مع تفاصيل تقنية قابلة للطي؛ TwinBadge على الجد المشترك في المشاركة.

### اختبارات / توثيق
- `occasionShare` (تحية + ICS).
- [`UPGRADE.md`](./UPGRADE.md) المرحلة 32.

---

## 2026-07-28 — نماذج/جدول زمني توأم + a11y أيقونات + إعداد الإطلاق

### المنتج
- TwinBadge في منتقي الأب/الأم والتوأم وجدول زمني التفاصيل.
- aria-label لأزرار الأيقونات في المناسبات والمقارنات والاكتشافات والمخطط.
- LanguageSwitcher وSetup محسّنان للإطلاق (`deploy:status` / تحديث التشخيص).

### اختبارات / توثيق
- `occasionsCsv` و`printFilter` (`applyNameMode` / `scopeSummaryLabel`).
- [`UPGRADE.md`](./UPGRADE.md) المرحلة 31.

---

## 2026-07-28 — تفاصيل/مسار/QR توأم + a11y مخطط + اختبارات

### المنتج
- TwinBadge على روابط العائلة في بطاقة التفاصيل ومسار التركيز وQR وملخص العائلة للطباعة.
- عناوين القبيلة/المنطقة والطباعة تستخدم `common.emDash`.
- aria-label لأزرار المخطط والجولات والتنقّل بين الإخوة.

### اختبارات / توثيق
- `spouseMeta` و`generationLimit` و`treeOccasions`.
- [`UPGRADE.md`](./UPGRADE.md) المرحلة 30.

---

## 2026-07-28 — منتقٍ/طباعة توأم + a11y إغلاق + زخارف طباعة

### المنتج
- TwinBadge في منتقي الأشخاص ولوحة التوائم وأوراق الطباعة (ملف شخص / مناسبة).
- إغلاق الحوارات والصفائح مترجم؛ عناوين القرابة تستخدم `common.emDash`.
- زخارف الطباعة بـ CSS بدل رمز ✦.

### اختبارات / توثيق
- `favoriteRelates` و`recentRelates` و`researchTour`.
- [`UPGRADE.md`](./UPGRADE.md) المرحلة 29.

---

## 2026-07-28 — مسارات/نواقص/جولات توأم + a11y + اختبارات عرض

### المنتج
- TwinBadge على مسار البيت ومسار المشاركة وشريط النواقص وجولتي البحث والاتساق.
- Spinner والشريط الجانبي مترجمان؛ وصف الفاتورة يستخدم `emDash`.

### اختبارات / توثيق
- `personDisplay` و`consistencyTourState` وحواف `researchTourState`.
- [`UPGRADE.md`](./UPGRADE.md) المرحلة 28.

---

## 2026-07-28 — مناسبات/مشاركة/اكتشافات + شهادة قرابة + اختبارات تخزين

### المنتج
- TwinBadge في لوحة المناسبات وحوار المشاركة والاكتشافات وشهادة القرابة.
- شرطات فارغة في الحساب والمشرف والفواتير عبر `common.emDash`.
- AuthLayout يترجم عناوين التنقّل والدخول.

### اختبارات / توثيق
- اختبارات `recentPeople` و`dismissedDiscoveries` و`occasionsScope` و`dismissedTodayEvents`.
- [`UPGRADE.md`](./UPGRADE.md) المرحلة 27.

---

## 2026-07-28 — أماكن/صور/مناسبات توأم + مروحة + روابط

### المنتج
- TwinBadge في الأماكن والصور وشريط المناسبات وبانر اليوم.
- مروحة الأسلاف: عناوين إضافة أب/أم وتمييز التوائم؛ قائمة الأشخاص تستخدم `emDash`.
- بادئة مسار القرابة وجيل الطباعة الصاعد عبر i18n.

### اختبارات / توثيق
- اختبارات `treeUrl` و`homePerson` و`favoritePeople`.
- README يذكر `prod:smoke` و`deploy:status` · [`UPGRADE.md`](./UPGRADE.md) المرحلة 26.

---

## 2026-07-28 — تفاصيل/مسارات قرابة + طباعة جنس + 404

### المنتج
- ورقة التفاصيل وشريطا مقارنات القرابة يعرضان TwinBadge؛ الجنس بلا رموز ♀♂.
- بطاقات الطباعة تترجم الجنس؛ شرطة فارغة عبر `common.emDash`.
- تسجيل الدخول بلا نص عربي ثابت؛ صفحة 404 تنبّه عند غياب القاعدة أو تأخر البناء.

### اختبارات / تشغيل
- `buildOccasionIcs` و`personDisplayNameWithTwin`؛ ملاحظة `prod:smoke` عن بصمة البناء في DEPLOY.

### توثيق
- [`UPGRADE.md`](./UPGRADE.md) المرحلة 25.

---

## 2026-07-28 — مساحة العمل/طباعة/مشاركة + توأم في البحث والنسب

### المنتج
- تنبيه الجاهزية على مساحة العمل والطباعة والمشاركة العامة.
- شارات توأم في حديثاً/المفضلة/بحث المخطط وكبسولات النسب والذرية.
- شعار التحويل البنكي أيقونة Landmark؛ زخارف المخطوط/الزخرفة مخفية عن قارئات الشاشة.

### اختبارات / تشغيل
- اختبارات اكتشاف `possibleTwin`؛ `prod:smoke` يفشل إن غابت بصمة البناء.

### توثيق
- [`UPGRADE.md`](./UPGRADE.md) المرحلة 24 · [`app/DEPLOY.md`](./app/DEPLOY.md).

---

## 2026-07-28 — جاهزية صفحات + توأم في القوائم + طباعة بلا إيموجي

### المنتج
- صفحات نجاح الدفع والأعضاء وقبول الدعوة تنبّه عند غياب القاعدة أو تأخر البناء.
- قائمة الأشخاص وشريط العائلة المباشرة يعرضان TwinBadge للتوائم.
- قالب المناسبات ومعايناته (قبيلة/مناسبة/زخرفة) بلا إيموجي — Lucide فقط.

### طباعة / i18n
- أسهم القبيلة والنخلة عبر أيقونات؛ افتراضي علامة التوأم في النخلة من `twinMarkWord`.
- ErrorBoundary ونصوص تهاني المناسبات بلا إيموجي ومن القاموس.

### اختبارات
- تغطية `isFullSibling` / مرشحي التوأم و`computePersonRanks` / `formatSiblingLabel`.

### توثيق
- [`UPGRADE.md`](./UPGRADE.md) المرحلة 23.

---

## 2026-07-28 — مسار قرابة توأم + إدارة/دفع + غلاف كتاب

### المنتج
- أداة «كيف يرتبطان؟»: شارات توأم على المسار، ربط/فتح توأم من النواقص، وشريط العائلة يرث people/rels.
- لوحة المشرف وصفحة الدفع تنبّهان عند غياب القاعدة أو تأخر البناء.
- غلاف الكتاب ومعاينته بلا إيموجي؛ معاينة النخلة تعرض علامة توأم.

### تشغيل / i18n
- تسمية العملة `common.currencyOmr`؛ ErrorBoundary يتبع لغة الصفحة.

### توثيق
- [`UPGRADE.md`](./UPGRADE.md) المرحلة 22.

---

## 2026-07-28 — معاينات طباعة + حساب + كتاب + دخان أقوى

### المنتج
- معاينات القوالب والصّفحة الشمسية مترجمة؛ معاينات المروحة/الشمس تعرض علامة توأم.
- صفحة الحساب تنبّه عند غياب القاعدة أو تأخر البناء.
- كتاب الطباعة يعرض سنوات الحياة وحالة المتوفى.
- نموذج الإضافة يبني النسب بـ بن/بنت حسب اللغة.

### تشغيل
- `prod:smoke` يشترط الصفحات والـ headers وردّ مسار الدخول — وليس health فقط.
- ملاحظات Ops/DEPLOY حول `continue-on-error` لمزامنة SHA.

### توثيق
- [`UPGRADE.md`](./UPGRADE.md) المرحلة 21.

---

## 2026-07-28 — شمس/مشاركة/لوحة تحكم + i18n طباعة

### المنتج
- قالب الشمس يترجم تسميات الزوج/الزوجة.
- مشاركة الرابط: شريط النواقص يدعم إظهار المسار وفتح التوأم المحتمل.
- ملخص واتساب للنواقص يذكر اسم التوأم المحتمل.
- لوحة التحكم تنبّه عند غياب القاعدة أو تأخر البناء.

### طباعة
- بسملة المخطوط وفاصل «بن» في التراث وaria النخلة عبر i18n.

### توثيق
- [`UPGRADE.md`](./UPGRADE.md) المرحلة 20.

---

## 2026-07-28 — توأم إنجليزي + فجوات توأم + ملاحظات زوجية

### المنتج
- علامات التوائم في أسماء الطباعة تتبع اللغة (`T1` بالإنجليزية، `ت1` بالعربية).
- شريط نواقص الشخص: ربط كتوائم / إظهار المسار / فتح التوأم المحتمل.
- قائمة سجل النسب تعرض علامات التوائم؛ ملاحظات الزوج/الزوجة مترجمة.

### تشغيل
- `launch:status` لا يفشل بسبب Neon المحلي (استشاري) — يعتمد دخان الإنتاج.

### توثيق
- [`UPGRADE.md`](./UPGRADE.md) المرحلة 19.

---

## 2026-07-28 — اكتشاف توأم + طباعة i18n + Setup موحّد

### المنتج
- اكتشاف «توأم محتمل»: أزرار ربط كتوائم، إظهار المسار، وفتح الشخص الآخر.
- مروحة/شمس الطباعة وأسطورة الشمس تستخدم `twins.mark` حسب اللغة.
- اختيار جذر الطباعة يعرض علامات التوائم؛ بطاقة الشخص المطبوعة تترجم «توأم/متوفى».

### تشغيل
- `/setup` يعتمد `useBuildBehind` لمقارنة البناء مع GitHub `main`.

### توثيق
- [`UPGRADE.md`](./UPGRADE.md) المرحلة 18.

---

## 2026-07-28 — ضيوف بقرابة + نخلة توأم + تنبيه الدخول

### المنتج
- قائمة ضيوف المناسبات تعرض القرابة وتاريخ الميلاد مع أسماء التوائم.
- قالب النخلة: علامات توأم على الجذع والسعف وقائمة الفيضان.

### تشغيل
- خطاف `useBuildBehind` مشترك؛ `/login` ينبّه إن تأخر البناء عن `main`.
- `prod:smoke` يفحص الصفحة الرئيسية `/`.

### اختبارات
- `birthOrder.test.ts` و`relationshipLabel.test.ts`.

### توثيق
- [`UPGRADE.md`](./UPGRADE.md) المرحلة 17.

---

## 2026-07-28 — قائمة ضيوف المناسبات + تنبيه الرئيسية

### المنتج
- قالب الطباعة «مناسبات»: قائمة أقارب/ضيوف من العائلة القريبة، مرتبة بالميلاد مع أسماء التوائم.
- الرئيسية (`/`) تعرض تنبيهاً ناعماً مع رابط `/setup` إن كان البناء الحي متأخراً عن GitHub `main`.

### تشغيل
- `prod:smoke` يقارن SHA الحي مع GitHub `main` ويوجّه لـ Redeploy عند التأخر.

### توثيق
- [`UPGRADE.md`](./UPGRADE.md) المرحلة 16.

---

## 2026-07-28 — كشف تأخر النشر + تراث

### تشغيل
- `npm run deploy:status` يقارن `/api/health` build مع `origin/main`.
- `/setup` ينبّه إن كان البناء الحي متأخراً عن GitHub `main`.
- Ops workflow يفحص مزامنة SHA يومياً.

### المنتج
- شريط نسب قالب التراث يستخدم `personDisplayNameWithTwin`.

### توثيق
- [`UPGRADE.md`](./UPGRADE.md) المرحلة 15 — خطوات Redeploy عند توقف النشر التلقائي.

---

## 2026-07-28 — قبيلة/ملصق + اختبار نخلة + بصمة CI

### المنتج
- `personDisplayNameWithTwin` لقالب القبيلة والملصق (والخريطة).
- اختبار ترتيب توائم أوراق النخلة.

### تشغيل
- `npm run assert:fingerprint` + خطوة CI للتحقق من حقن `NASAB_BUILD_SHA`.
- تنبيه: إن تأخر SHA الحي عن `main` → Redeploy يدوي من Vercel.

### توثيق
- [`UPGRADE.md`](./UPGRADE.md) المرحلة 14.

---

## 2026-07-28 — مخطوط/خريطة + ترتيب توأم + أمان Ops

### المنتج
- شارات توأم في ميداليات المخطوط وأسطورة الخريطة.
- سطور الترتيب في الشجرة تعرض تسمية التوأم عند الربط.

### تشغيل / أمان
- توثيق حدود rate-limit وheaders في DEPLOY.
- `npm run vercel:env-check` و`--checklist` لطباعة جاهزية المتغيرات دون إجهاض صامت.
- `prod:smoke` يتحقق من `X-Content-Type-Options` / `X-Frame-Options`.

### توثيق
- [`UPGRADE.md`](./UPGRADE.md) المرحلة 13.

---

## 2026-07-28 — نخلة عُمان + اكتشافات توأم + Setup i18n

### المنتج
- قالب النخلة: ترتيب بالميلاد وعلامات توائم على الأسماء.
- اكتشاف «توأم محتمل» في لوحة Discoveries.
- صفحة `/setup` بالكامل عبر مفاتيح i18n.

### تشغيل
- `prod:smoke` يفحص `/setup`.

### توثيق
- [`UPGRADE.md`](./UPGRADE.md) المرحلة 12.

---

## 2026-07-28 — نواقص توأم + تدوير كلمة المرور

### المنتج
- فجوة بحث «توأم محتمل» للإخوة الأشقاء بنفس سنة الميلاد.
- شارة توأم في صفحات قالب الكتاب المطبوعة.

### تشغيل / أمان
- `npm run admin:rotate` يولّد كلمة مرور دخول جديدة (env فقط) واختياريًا يكتب `.env.production`.

### توثيق
- [`UPGRADE.md`](./UPGRADE.md) المرحلة 11.

---

## 2026-07-28 — ترتيب مخطط مستقر + Ops مجدول

### المنتج
- توحيد ترتيب الميلاد المستقر في المخطط والطباعة وعرض الأحفاد.
- شارة نوع التوأم في لوحة التوائم.

### تشغيل
- workflow يومي `.github/workflows/ops.yml`: دخان إنتاج + نسخ Neon (سر `DATABASE_URL`).

### توثيق
- [`UPGRADE.md`](./UPGRADE.md) المرحلة 10 · [`app/DEPLOY.md`](./app/DEPLOY.md).

---

## 2026-07-28 — دورة توائم GEDCOM

### المنتج
- تصدير التوائم في GEDCOM عبر `_TGID` وروابط `ASSO` / `RELA twin`.
- استيراد يعيد بناء `twinGroupId` داخل الشجرة.
- معاينة الاستيراد تعرض عدد مجموعات التوائم.

### توثيق
- [`UPGRADE.md`](./UPGRADE.md) المرحلة 9 · تحديث README.

---

## 2026-07-28 — توائم مستقرة + صقل الدخول والجاهزية

### المنتج
- ترتيب ميلاد مستقر بالمعرّف عند تساوي التاريخ (التوائم).
- نوع توأم «مختلط» للأجناس المختلفة + شارة وشريط عائلة مباشرة.
- نموذج دخول يدعم Enter؛ تعطيل الإرسال عند `dbConfigured=false`.
- إخفاء Kimi إن لم يُضبط `KIMI_AUTH_URL`.

### الأمان / الإطلاق
- `/api/diag` لم يعد يعيد بريد المشرف — أعلام حضور فقط (+ `APP_PUBLIC_URL` / `ALLOWED_ORIGINS`).
- زر نسخ أوامر Vercel في `/setup`.

### توثيق
- [`UPGRADE.md`](./UPGRADE.md) المرحلة 8.

---

## 2026-07-28 — بصمة البناء وحالة الإطلاق

### الجديد
- حقن `NASAB_BUILD_SHA` و`NASAB_BUILD_TIME` أثناء `vercel-build`.
- `/api/health` و`/api/diag` و`/setup` تعرض بصمة البناء.
- رابط «جاهزية الإطلاق» في تذييل الصفحة الرئيسية.
- `npm run launch:status` لدمج فحص Neon المحلي وفحص الدخان الحي.

### توثيق
- [`UPGRADE.md`](./UPGRADE.md) المرحلة 6 و7.
- أوامر `launch:status` / `vercel:print-env` في [`README.md`](./README.md) و[`app/DEPLOY.md`](./app/DEPLOY.md).

---

## 2026-07-28 — صفحة الجاهزية + نسخ Neon + CI smoke

### الجديد
- صفحة `/setup` تعرض حالة الربط (DATABASE_URL / APP_SECRET / sidecar).
- رابط من `/login` إلى الجاهزية عند غياب القاعدة.
- `npm run db:backup-neon` لنسخة JSON من الجداول الأساسية.
- فحص دخان اختياري في GitHub Actions (`prod:smoke`).

### توثيق
- تحديث [`UPGRADE.md`](./UPGRADE.md) المرحلة 5.

---

## 2026-07-28 — استمرار الترقية: جسم الطلب + أدوات الإطلاق

### الإصلاح
- `shouldAddHelpers: false` في دالة Vercel لتفادي تعليق قراءة جسم POST.
- دخول `/login` يرسل `application/x-www-form-urlencoded`.
- `npm run prod:smoke` لفحص الصحة/التشخيص/الدخول على النطاق الحي.
- `npm run vercel:print-env` لطباعة متغيرات اللصق في لوحة Vercel.

### توثيق
- تحديث [`UPGRADE.md`](./UPGRADE.md) المرحلة 4.

---

## 2026-07-28 — خطة ترقية + ربط Neon الحقيقي

### قاعدة البيانات (Neon) — تم التحقق محلياً
- 18 جدولاً، مشرف `admin@bhd.om`، خطط (3)، بوابات دفع (5).
- أوامر: `npm run db:verify-neon`، `npm run db:seed-neon`، `npm run admin:ensure`.

### المنتج
- خطة التنفيذ في [`UPGRADE.md`](./UPGRADE.md).
- تنبيه في `/login` عند غياب `DATABASE_URL` على الخادم.
- `vercel:env` يولّد `APP_SECRET` إن نقص.

### متبقٍ (يدوي على Vercel)
- ضبط `DATABASE_URL` + `APP_SECRET` ثم Redeploy — المرحلة 2 في UPGRADE.md.

---

## 2026-07-28 — إصلاح انهيار دالة Vercel + نسخ postgres

### السبب
- استيراد `postgres` / Neon بشكل ثابت عند تحميل الوحدة كان يُسقط الدالة (`FUNCTION_INVOCATION_FAILED`) على Vercel.
- `require` الديناميكي يترك الحزم خارج حزمة esbuild، فلا تجدها دالة Build Output.

### الإصلاح
- العودة للتحميل الكسول لسائق Postgres (كما كان يعمل `/api/health`).
- `vercel-build.mjs` ينسخ `node_modules/postgres` و`drizzle-orm` داخل `api.func`.
- الإبقاء على مهلة تسجيل الدخول ورفض `DATABASE_URL` الفارغ.

### تحقق محلي (cwd = api.func)
- health → 200، loginLocal → 200.

---

## 2026-07-28 — دخول مباشر عبر Hono + تشخيص Vercel

### اكتشاف
- `/api/diag` أظهر أن **Production على Vercel بلا `DATABASE_URL` ولا `APP_SECRET`**.
- إجراءات tRPC ذات `.input(zod)` تعلق على Vercel Build Output (بدون رد)، بينما الإجراءات بلا input تعمل.
- السبب الأقرب: مسار قراءة جسم الطلب عبر بعض الطبقات يعلّق؛ لذلك الدخول صار عبر Hono مع فحوصات سريعة.

### الإصلاح
- مسار جديد: `POST /api/auth/password-login` (Hono) يتجاوز tRPC.
- صفحة `/login` تستدعي هذا المسار مباشرة.
- الإبقاء على sidecar `db-pg.cjs` (Neon HTTP) لتسجيل الدخول بعد ضبط `DATABASE_URL`.
- عند غياب `DATABASE_URL` يرد المسار فوراً بـ 503 واضحة.

### مطلوب منك على Vercel (إلزامي)
في Project → Settings → Environment Variables (Production) ثم **Redeploy**:

```
DATABASE_URL=postgresql://...-pooler.../neondb?sslmode=require
APP_SECRET=<32+ حرفاً عشوائياً>
PASSWORD_LOGIN_EMAIL=admin@bhd.om
PASSWORD_LOGIN_PASSWORD=Admin@1234
OWNER_UNION_ID=password:admin@bhd.om
APP_PUBLIC_URL=https://nasab-mu.vercel.app
ALLOWED_ORIGINS=https://nasab-mu.vercel.app
```

تحقق: `GET /api/diag` → `dbConfigured: true` ثم دخول من `/login`.

أو بعد `vercel login`:
```bash
cd app
npm run vercel:env
```
ثم Redeploy.

---

## 2026-07-28 — Postgres sidecar لـ Vercel بدون كسر Cold Start

### الإصلاح
- إعادة خط الأساس الصحي لـ `/api/health` بعد انهيارات بسبب استيراد السائق عند التحميل.
- بناء `db-pg.cjs` بجانب الدالة وتحميله فقط داخل `getDb()` عند الحاجة.
- الـ sidecar يستخدم **Neon HTTP** (`@neondatabase/serverless`) لتفادي تعليق TCP من serverless.
- مهلة واضحة لتسجيل الدخول إن لم تستجب قاعدة البيانات.

### تحقق
- محلياً (حزمة Vercel): health 200 + loginLocal 200 عبر Neon HTTP.
- حي: بعد النشر يبقى health 200؛ الدخول يحتاج `DATABASE_URL` على Vercel.

---

## 2026-07-28 — تضمين postgres.js في دالة Vercel

### الإصلاح
- `postgres.js` يُستورد بشكل ثابت (`pg-node.ts`) حتى يضمّنه esbuild داخل `.vercel/output` (الـ `require` الديناميكي كان يترك الحزمة خارج الدالة).
- إزالة `@neondatabase/serverless` مؤقتاً بعد أن تسبب استيراده في `FUNCTION_INVOCATION_FAILED` على Vercel.
- `/api/health` يعرض `dbConfigured` دون فحص DB ثقيل عند كل طلب.
- الإبقاء على مهلة `auth.loginLocal` ورفض `DATABASE_URL` الفارغ.

### تحقق محلي على الحزمة
- `health` → 200، `auth.loginLocal` → 200 + cookie مع Neon pooled.

---

## 2026-07-28 — Neon HTTP على Vercel + فحص صحة أوضح

### الإصلاح
- على Vercel/serverless: اتصال Postgres عبر `@neondatabase/serverless` (HTTP) بدل `postgres` TCP لتجنب تعليق تسجيل الدخول.
- **تضمين Neon داخل حزمة الدالة** عبر استيراد ثابت (`pg-neon.ts`) — `require` الديناميكي كان يترك الحزمة خارج الـ bundle فيفشل التشغيل على Vercel.
- رفض `DATABASE_URL` الفارغ بدل السقوط صامتاً على MySQL.
- `/api/health` يعرض `dbConfigured` / `dialect`؛ و`?db=1` يفحص الاتصال بمهلة قصيرة.
- مهلة واضحة لـ `auth.loginLocal` مع رسالة تطلب ضبط `DATABASE_URL` على Vercel.
- سكربت `npm run vercel:env` لمزامنة المتغيرات من `.env.production` إلى Vercel.

### التحقق المحلي
- الحزمة المجمّعة: `GET /api/health?db=1` → `db:ok` و`auth.loginLocal` → 200 + cookie.
- Neon pooled يصل من الجهاز؛ صف المشرف `admin@bhd.om` محدّث عبر `npm run admin:ensure`.
- الموقع الحي يحتاج `DATABASE_URL` على مشروع Vercel ثم إعادة نشر بعد هذا الإصلاح.

---

## 2026-07-28 — إصلاح 504 للـ API على Vercel

### الإصلاح
- استبدال `hono/vercel` بـ `getRequestListener` من `@hono/node-server` (متوافق مع Node launcher).
- تفعيل `shouldAddHelpers` في `.vc-config.json`.
- تأجيل بذر المنصة إلى خلفية أول طلب بدل التحميل عند الاستيراد (يمنع التعليق).
- التحقق المحلي: `GET /api/health` → 200.

### حالة الأدمن
- `admin@bhd.om` / `Admin@1234` موجود في Neon كـ `role=admin`.

---

### الجديد / الإصلاح
- دخول مشرف ثابت: **admin@bhd.om** / **Admin@1234** (`PASSWORD_LOGIN_*` + `OWNER_UNION_ID=password:admin@bhd.om`).
- نموذج البريد يظهر دائماً على `/login`.
- دالة Vercel تُبنى كاملة (CommonJS + deps) عبر Build Output API.
- سكربت `npm run admin:ensure` لإنشاء المشرف في Neon.
- بذر الخطط/البوابات مقاوم للتزامن على Cold Start.

### توثيق
- `app/DEPLOY.md`, `README.md`, `.env.production.example`

---

### الجديد
- دعم **PostgreSQL / Neon** بجانب SQLite و MySQL (`db/schema.pg.ts`, dialect, drizzle).
- نقل مصدر الـ API من `api/` → **`server/`** لتجنب أخطاء TypeScript على Vercel.
- بناء دالة serverless واحدة: `api/index.js` من `server/vercel.ts` (عبر `hono/vercel`).
- تحميل كسول لمشغّلات DB حتى لا يتعطل `npm run dev` على SQLite.
- توثيق النشر في `README.md` و `app/DEPLOY.md` و `app/README.md`.

### الأوامر
- `npm run build` → واجهة + `dist/boot.js` + `api/index.js`
- `npm run db:push` مع `DATABASE_URL=postgresql://...`

---

## 2026-07-26 — مخططات مشاركة + اكتمال + دليل نمو

### الجديد
- صفحة **/share**: تبديل بين عائلة / أسلاف / مروحة / أحفاد.
- بطاقة **اكتمال الشجرة** الشفافة للزوار.
- **دليل نمو** للقراءة فقط (بدون أزرار تعديل).

### الملفات
- `ShareCompletenessCard.tsx`, تحديثات `ShareView`, `TreeGrowthChecklist` + ترجمات

---

## 2026-07-26 — QR + صور مشاركة + CSV مناسبات + واتساب نواقص

### الجديد
- **رمز QR** لرابط الشخص (توليد محلي + طباعة/تنزيل) في مساحة العمل و/share.
- صفحة **/share**: معرض صور + مسار إلى شخص المقارنة + نواقص عبر واتساب.
- تصدير مناسبات **CSV** + مشاركة نواقص الملف عبر واتساب.

### الملفات
- `PersonShareQrDialog.tsx`, `researchDigest.ts`, `occasionsCsv.ts`
- تحديثات `PersonGapsStrip`, `EventsStrip`, `TreeWorkspace`, `ShareView` + ترجمات + اعتماد `qrcode`

---

## 2026-07-26 — ترتيب إخوة + أماكن مشاركة + واتساب بطاقة/ملخص

### الجديد
- شريط **الإخوة حسب الميلاد** مع ترتيب وعمر وتمييز الشخص الحالي.
- صفحة **/share**: متصفّح أماكن العائلة + نواقص الملف + شريط الترتيب.
- **واتساب** لبطاقة الشخص وملخص العائلة (مساحة العمل + المشاركة).

### الملفات
- `BirthOrderStrip.tsx`, تحديثات `relationShare`, `EventsStrip`, `TreeWorkspace`, `ShareView` + ترجمات

---

## 2026-07-26 — عمر وترتيب + تصدير vCard/JSON + اكتشافات على المشاركة

### الجديد
- شريط العائلة المباشرة يعرض **العمر/مدى الحياة** وترتيب الإخوة (مثل ٢/٥).
- تنزيل **vCard** و**JSON** من ملف الشخص (مساحة العمل + /share).
- لوحة **اكتشافات** للقراءة فقط على صفحة المشاركة مع إبراز المسار.

### الملفات
- `personExport.ts`, `birthOrder.ts` (عمر/ترتيب), `ImmediateFamilyStrip.tsx`
- تحديثات `TreeWorkspace`, `ShareView` + ترجمات

---

## 2026-07-26 — واتساب بنقرة + تقويم مشاركة + ورقة ملخص

### الجديد
- **واتساب بنقرة** للتهاني ومسار القرابة (شريط المناسبات + «كيف يرتبطان؟» + /share).
- صفحة **/share**: بانر مناسبات اليوم + تقويم مناسبات كامل (عرض الكل).
- **ورقة ملخص العائلة** قابلة للطباعة (اليوم + الأسبوع + أولويات البحث).

### الملفات
- `whatsAppShare.ts`, `FamilyBriefPrintDialog.tsx`
- تحديثات `EventsStrip`, `OccasionsPanel`, `TodayEventsBanner`, `RelationPathDialog`, `TreeWorkspace`, `ShareView` + ترجمات

---

## 2026-07-26 — ذكرى وفاة + بطاقة مناسبة + شريط مقارنات أخيرة

### الجديد
- مناسبات **ذكرى الوفاة** من يوم/شهر الوفاة (تقويم، تهنئة، ملخص، ICS).
- **بطاقة مناسبة** قابلة للطباعة من الشريط/التبويب بدل القفز لملصق الطباعة فقط.
- شريط **مقارنات أخيرة** فوق مساحة العمل (فتح / إبراز / شهادة / تثبيت).

### الملفات
- `treeOccasions.ts`, `OccasionCardPrintDialog.tsx`, `RecentRelatesStrip.tsx`
- تحديثات `EventsStrip`, `OccasionsPanel`, `TodayEventsBanner`, `TreeWorkspace`, `ShareView` + ترجمات

---

## 2026-07-26 — طباعة المشاركة + شريط مقارنات مثبتة + جولة اتساق

### الجديد
- **/share**: طباعة شهادة القرابة وورقة الملف + شارة الجد المشترك على المسار.
- شريط **مقارنات مثبتة** فوق مساحة العمل (فتح / إبراز / طباعة).
- **جولة فحص الاتساق** (تواريخ/تكرار) بنطاق واستئناف واختصار `C`.

### الملفات
- `FavoriteRelatesStrip.tsx`, `ConsistencyTourStrip.tsx`, `consistencyTour.ts`, `consistencyTourState.ts`
- تحديثات `ShareView`, `TreeWorkspace`, `ShortcutsDialog` + ترجمات

---

## 2026-07-26 — جد مشترك + ورقة ملف + مقارنات مثبتة

### الجديد
- **يلتقيان عند…** جد/جدة مشتركة في «كيف يرتبطان؟» والشهادة ونص المسار.
- **ورقة ملف شخص** قابلة للطباعة (عائلة مباشرة + نواقص + مسار البيت + رابط).
- **تثبيت مقارنات القرابة** (نجوم) فوق المقارنات الأخيرة.

### الملفات
- `favoriteRelates.ts`, `PersonProfilePrintDialog.tsx`, `findCommonAncestorId`
- تحديثات `RelationPathDialog`, `KinshipCertificateDialog`, `relationShare`, `TreeWorkspace` + ترجمات

---

## 2026-07-26 — ملخص بحث + نطاق مشاركة + شهادة قرابة

### الجديد
- ملخص العائلة يتضمن **أولويات بحث** (أعلى ٥ نواقص + روابط عميقة) حسب نطاق الجولة.
- صفحة **/share**: نطاق مناسبات (دائرتي / مفضلة / الكل) محفوظ محلياً.
- **شهادة قرابة** قابلة للطباعة/PDF من «كيف يرتبطان؟» مع المسار والرابط.

### الملفات
- `familyBrief.ts`, `occasionsScope.ts`, `KinshipCertificateDialog.tsx`
- تحديثات `RelationPathDialog`, `ShareView`, `TreeWorkspace` + ترجمات

---

## 2026-07-26 — مناسبات المشاركة + ملفّان في المقارنة + جولة بحث بنطاق

### الجديد
- **/share**: شريط مناسبات حيّ (تهنئة / تقويم / ملخص عائلي) بروابط المشاركة العامة.
- «كيف يرتبطان؟»: **ملفّان مصغّران** (نواقص + عائلة مباشرة) + نسخ البطاقتين معاً.
- جولة البحث: نطاق **دائرتي / مفضلة / الكل** مع **استئناف الفهرس** بعد إعادة التحميل.

### الملفات
- `researchTourState.ts`, تحديثات `researchTour.ts`, `ResearchTourStrip`, `RelationPathDialog`, `ShareView`, `OccasionsScopeChips`, `TreeWorkspace` + ترجمات

---

## 2026-07-26 — مشاركة عامة أغنى + مقارنات أخيرة + ملخص عائلي

### الجديد
- صفحة **/share**: شريط مسار القرابة، نسخ نص المسار/بطاقة الشخص، والعائلة المباشرة في بطاقة الشخص.
- **مقارنات أخيرة** في «كيف يرتبطان؟» (محفوظة محلياً لكل شجرة).
- زر **نسخ ملخص العائلة**: مناسبات اليوم + ٧ أيام (حسب النطاق) + عدد نواقص البحث، جاهز لواتساب.

### الملفات
- `recentRelates.ts`, `familyBrief.ts`, `ShareView.tsx`, `RelationPathDialog.tsx`, `EventsStrip.tsx`, `TreeWorkspace.tsx` + ترجمات

---

## 2026-07-26 — نص مسار/بطاقة شخص + نطاق مناسبات + تقويم ٩٠ يوماً

### الجديد
- **نسخ نص مسار القرابة** و**بطاقة شخص** (واتساب/رسائل) مع القرابة والرابط العميق.
- نطاق المناسبات: **دائرتي / المفضلة / الكل** (يُحفظ محلياً) على البانر والشريط وتبويب المناسبات.
- تنزيل **ملف تقويم واحد** لمناسبات الـ٩٠ يوماً القادمة ضمن النطاق المختار.

### الملفات
- `relationShare.ts`, `occasionsScope.ts`, `OccasionsScopeChips.tsx`
- تحديثات `occasionShare.ts`, `PathToHomeStrip`, `RelationPathDialog`, `EventsStrip`, `OccasionsPanel`, `TreeWorkspace` + ترجمات

---

## 2026-07-26 — رابط مسار القرابة + جولة بحث + تقويم/تهنئة

### الجديد
- روابط عميقة `?person=&relate=` تفتح المسار مضيئاً؛ نسخ الرابط من «كيف يرتبطان؟» ومسار البيت.
- **جولة بحث** فوق المخطط: إصلاح / إظهار / تخطّي + اختصار `N`.
- مناسبات: تنزيل `.ics` ونسخ تهنئة مع رابط الشخص.

### الملفات
- `treeUrl.ts`, `researchTour.ts`, `occasionShare.ts`, `ResearchTourStrip.tsx`
- تحديثات `RelationPathDialog`, `PathToHomeStrip`, `EventsStrip`, `OccasionsPanel`, `TodayEventsBanner`, `ShareView`, `TreeWorkspace`, `ShortcutsDialog` + ترجمات

---

## 2026-07-26 — نقاط بحث على المخطط + مناسبات اليوم

### الجديد
- **نقاط بحث كهرمانية** على بطاقات المخطط/الأسلاف/الأحفاد من نواقص الملف، مع أزرار إصلاح في معاينة التحويم.
- **بانر مناسبات اليوم** (قابل للإخفاء لليوم) فوق شريط المناسبات.
- شريط المناسبات: **طباعة** و**نسخ رابط الشخص** بجانب كل بطاقة.

### الملفات
- `personGaps.ts`, `dismissedTodayEvents.ts`, `TodayEventsBanner.tsx`
- تحديثات `FamilyChart`, `MhPersonPill`, `PedigreeView`, `DescendantsView`, `EventsStrip`, `TreeWorkspace` + ترجمات

---

## 2026-07-26 — أماكن + مناسبات + شارة قرابة ومعرض مرتّب

### الجديد
- تبويب **الأماكن**: تجميع بالميلاد/الوفاة، شارة قرابة، انتقال للمخطط، وطباعة خريطة.
- تبويب **المناسبات**: تقويم السنة كاملة مع العدّ التنازلي وطباعة قالب المناسبات.
- **شارة قرابة** تحت اسم الملف (نقر = إبراز المسار) + معرض صور مرتّب بيت/مفضلة/أخيرون.

### الملفات
- `PlacesBrowser.tsx`, `OccasionsPanel.tsx`, `treeOccasions.ts`
- تحديثات `EventsStrip`, `PhotosGallery`, `TreeWorkspace`, `TreePrint`, `treeUrl` + ترجمات

---

## 2026-07-26 — بحث فوري شخصي + مسار البيت + اكتشافات قابلة للإخفاء

### الجديد
- **بحث المخطط**: ترتيب شخص البيت → المفضلة → الأخيرون، شارة قرابة، واقتراحات عند الحقل الفارغ.
- **مسار من شخص البيت** في لوحة التفاصيل (hops قابلة للنقر + إبراز على المخطط).
- **اكتشافات**: إخفاء فردي، ترتيب قرب البيت/المفضلة، وإظهار المخفي عند الحاجة.

### الملفات
- `ChartPersonSearch.tsx`, `PathToHomeStrip.tsx`, `DiscoveriesPanel.tsx`
- `dismissedDiscoveries.ts`, `discoveries.ts` (ترتيب)، `TreeWorkspace.tsx` + ترجمات

---

## 2026-07-26 — روابط عميقة + مفضلة + قائمة نمو

### الجديد
- **روابط عميقة** `?person=&view=&tab=` في مساحة العمل، و`?person=` في المشاركة العامة، و`?root=` في الطباعة — مع نسخ رابط الشخص/المشاركة وطباعة من الملف.
- شريط **المفضلة** (تثبيت متعدد) بجانب «شاهدتهم مؤخراً»، مع رفع المفضلة في بحث المخطط.
- **قائمة نمو الشجرة** للأشجار الصغيرة/الناقصة + تفصيل مؤشر الاكتمال بالنقر على الشريط.

### الملفات
- `treeUrl.ts`, `favoritePeople.ts`, `FavoritesStrip.tsx`, `TreeGrowthChecklist.tsx`
- تحديثات `TreeWorkspace.tsx`, `ShareView.tsx`, `TreePrint.tsx`, `TreeHomeBanner.tsx`, `ChartPersonSearch.tsx` + ترجمات

---

## 2026-07-26 — توحيد التنقّل عبر أوضاع العرض

### الجديد
- **معاينة التحويم + تركيز** على كبسولات الأسلاف والأحفاد (مثل مخطط العائلة)، مع تلميح القرابة ونقر مزدوج في المروحة.
- زر **أظهر على المخطط** من القائمة ولوحة التفاصيل — يوحّد القفز عبر `revealOnChart` (تبويب المخطط + توسيط).
- اختصار **← →** للتنقّل بين الإخوة أثناء فتح التفاصيل (متوافق مع RTL).

### الملفات
- `MhPersonPill.tsx`, `PedigreeView.tsx`, `DescendantsView.tsx`, `FanChartView.tsx`
- `TreeWorkspace.tsx`, `ShortcutsDialog.tsx` + ترجمات

---

## 2026-07-26 — تركيز مزدوج + تواريخ الزواج + تنقّل الإخوة

### الجديد
- **نقر مزدوج** على البطاقة للتركيز، وأزرار «تركيز هنا / كيف يرتبطان؟» في معاينة التحويم.
- **نقر رابط الزواج** لتعديل سنة الزواج/الانفصال (API موجود مسبقاً).
- تنقّل **← → بين الإخوة** في لوحة التفاصيل + خط زمني قابل للنقر.
- موحّد **عدد الأجيال** لأسلاف/مروحة/أحفاد/عائلة.

### الملفات
- `SpouseDatesDialog.tsx`, تحديثات `ChartConnectors.tsx`, `FamilyChart.tsx`, `TreeWorkspace.tsx`, `ShortcutsDialog.tsx` + ترجمات

---

## 2026-07-26 — معاينة التحويم + نواقص الملف + مراجعة المكررات

### الجديد
- **معاينة عند التحويم** على بطاقات المخطط: صورة، سنوات، شارة القرابة للمحور، وفتح الملف.
- قائمة **ما ينقص هذا الملف** في التفاصيل مع أزرار إصلاح مباشرة.
- في الاكتشافات للمكررين: **كيف يرتبطان؟** / **أظهر المسار** / فتح الآخر.
- زر **كثافة العرض** (مريح / مضغوط) لمخطط العائلة.

### الملفات
- `personGaps.ts`, `PersonGapsStrip.tsx`
- تحديثات `FamilyChart.tsx`, `DiscoveriesPanel.tsx`, `RelationPathDialog.tsx`, `TreeWorkspace.tsx` + ترجمات

---

## 2026-07-26 — استيراد GEDCOM + أشخاص أخيرون + اختصارات

### الجديد
- **استيراد GEDCOM (.ged)** مع روابط الأب/الزوج — يكمل حلقة التصدير ويُقرّب تجربة MyHeritage.
- شريط **شاهدتهم مؤخراً** تحت غلاف الموقع للتنقّل السريع.
- **اختصارات لوحة المفاتيح**: `/` بحث، `Esc` إغلاق، `H` شخص البيت، `R` كيف يرتبطان، `?` الدليل.

### الملفات
- `gedcomImport.ts`, `GedcomImportDialog.tsx`, `RecentPeopleStrip.tsx`, `ShortcutsDialog.tsx`
- API: `person.importGedcom`
- تحديثات `TreeWorkspace.tsx` + ترجمات

---

## 2026-07-26 — فلتر الزيجات + اكتمال الشجرة + فلاتر القائمة

### الجديد
- في العائلات متعددة الزيجات: شرائح **كل الزيجات / زوجة أو زوج** لتصفية أعمدة الأبناء (أوضح من عرض الكل دائماً).
- مؤشر **اكتمال الشجرة** في غلاف الموقع (ميلاد، صور، آباء، روابط زوجية).
- فلاتر القائمة: جنس، حي/متوفى، غير مربوطين فقط + عدّاد النتائج.

### الملفات
- `treeCompleteness.ts`, تحديثات `FamilyChart.tsx`, `TreeHomeBanner.tsx`, `TreeWorkspace.tsx` + ترجمات

---

## 2026-07-26 — توسيط المخطط + إبراز مسار القرابة + سجل التركيز

### الجديد (أوضح من MyHeritage في التنقّل)
- **توسيط المحدد** و**ملاءمة العرض** داخل مخطط العائلة.
- البحث / شخص البيت / التركيز يحرّكان المخطط تلقائياً إلى الشخص.
- **عرض المسار على المخطط** من أداة «كيف يرتبطان؟» مع تمييز بنفسجي وتعتيم الباقي.
- **سجل التركيز** مع رجوع سريع وشريحة للزيارات السابقة.

### الملفات
- تحديثات `FamilyChart.tsx`, `RelationPathDialog.tsx`, `TreeWorkspace.tsx` + ترجمات

---

## 2026-07-26 — كيف يرتبطان؟ + شخص البيت

### الجديد
- أداة **كيف يرتبطان؟**: اختيار شخصين → تسمية القرابة (عم، ابن عم، أخ…) + مسار القفزات القابل للنقر.
- **شخص البيت**: تعيين من الملف والعودة إليه بزر في شريط المخطط (يُحفظ محلياً لكل شجرة).
- اختبارات وحدة لمسار القرابة.

### الملفات
- `relationPath.ts`, `RelationPathDialog.tsx`, `homePerson.ts`
- تحديثات `TreeWorkspace.tsx` + ترجمات

---

## 2026-07-26 — إضافة قرابة سريعة + عائلة مباشرة + أحفاد + جودة بيانات

### الجديد (أوضح من MyHeritage في نقاط)
- زر **+** يفتح قائمة صلة القرابة (أب/أم/زوج/ابن/…) بدل إضافة ابن فقط.
- شريط **العائلة المباشرة** بصور قابلة للنقر في لوحة التفاصيل.
- وضع عرض **الأحفاد** مقابل مخطط الأسلاف.
- اكتشافات جودة إضافية: وفاة قبل الميلاد، مولود قبل والده، أسماء مكررة محتملة، حي بلا سنة ميلاد.

### الملفات
- `QuickAddMenu.tsx`, `ImmediateFamilyStrip.tsx`, `DescendantsView.tsx`
- تحديثات `FamilyChart.tsx`, `discoveries.ts`, `TreeWorkspace.tsx`, ترجمات

---

## 2026-07-26 — اكتشافات + تصدير GEDCOM + ملء الشاشة

### الجديد (أقرب لأسلوب MyHeritage وأوضح منه)
- لوحة **اكتشافات**: آباء ناقصون، أبناء بلا رابط زوجية — مع أزرار إضافة مباشرة.
- **تصدير GEDCOM 5.5.1** من شريط أدوات الشجرة (قابل للاستيراد في برامج الأنساب).
- تحكم **عدد الأجيال** في عرض العائلة لتقليل الازدحام.
- وضع **ملء الشاشة** للمخطط مع خروج بـ Escape.
- اختبارات وحدة للتصدير والاكتشافات.

### الملفات
- `discoveries.ts`, `DiscoveriesPanel.tsx`, `gedcomExport.ts`, `generationLimit.ts`
- تحديثات `TreeWorkspace.tsx` + ترجمات ar/en

---

## 2026-07-26 — عائلة قريبة + مناسبات + شريط إجراءات الملف

### الجديد
- وضع **العائلة القريبة**: الأب/الأم + الإخوة + الزوج/ة + الأبناء فقط (مثل Close Family في MyHeritage).
- شريط **المناسبات القريبة**: أعياد ميلاد وذكريات زواج مرتبة زمنياً.
- في لوحة التفاصيل: أزرار **الملف / تعديل / إضافة / عائلة قريبة** بأسلوب مواقع النسب.
- اختبار وحدة لـ `collectCloseFamily`.

### الملفات
- `closeFamily.ts`, `EventsStrip.tsx`
- تحديثات `TreeWorkspace.tsx` + ترجمات

---

## 2026-07-26 — إضافة سريعة + معرض صور + تسميات القرابة

### تفاعل الشجرة
- زر **+** تحت بطاقة الشخص لإضافة قريب فوراً.
- خانات **إضافة أب/أم** في مخطط الأسلاف والمروحة تفتح نموذج الإضافة بالصلة الصحيحة.
- تبويب **الصور**: معرض لكل من لديهم صورة.
- عمود **القرابة** في القائمة نسبةً للشخص المحوري (أب، أم، ابن، أخ…).

### الملفات
- `PhotosGallery.tsx`, `relationshipLabel.ts`
- تحديثات `PersonFormDialog` (`defaultKinship`)، `FamilyChart`، `PedigreeView`، `FanChartView`، `TreeWorkspace`

---

## 2026-07-26 — رأس موقع العائلة + بحث + خط زمني

### صفحة الشجرة
- **غلاف بصري** مع اسم الشجرة والقبيلة/المنطقة.
- بطاقة **منشئ الموقع** + إحصائيات (ناس / صور / زيجات / أحياء) + نبذة.
- **بحث داخل المخطط** يفتح التفاصيل ويركّز على الشخص.
- **خط زمني** في لوحة التفاصيل: ميلاد، زواج، إنجاب، وفاة.
- تحسين خطوط **مخطط الأسلاف** (مفصلات متعامدة أوضح).

### الملفات
- `TreeHomeBanner.tsx`, `ChartPersonSearch.tsx`
- تحديثات `TreeWorkspace.tsx`, `PedigreeView.tsx`

---

## 2026-07-26 — أوضاع عرض متعددة (عائلة / أسلاف / مروحة)

### شريط أوضاع المخطط
- **عرض العائلة**: البطاقات العمودية والخطوط المتعامدة (السابق).
- **مخطط الأسلاف**: كبسولات أفقية أزرق/وردي، الشخص يميناً والأسلاف يساراً، خطوط بلا أسهم.
- **مظهر مروحي**: أقواس أجيال SVG قابلة للنقر مع تمييز القطاع المحدد.
- عدّاد «عرض X من Y شخص» أعلى المخطط.
- القائمة: صور دائرية بجانب الأسماء.

### الملفات الجديدة
- `app/src/components/tree/PedigreeView.tsx`
- `app/src/components/tree/FanChartView.tsx`
- `app/src/components/tree/MhPersonPill.tsx`

---

## 2026-07-26 — عرض عائلي بأسلوب MyHeritage (وأعلى وضوحاً)

### المخطط التفاعلي

- **بطاقات عمودية** بلون أزرق/وردي فاتح، صورة دائرية كبيرة، اسم وتواريخ في الوسط — بلا ازدحام.
- **خطوط رمادية رفيعة متعامدة** من منتصف الزوجين للأبناء — **بدون أسهم وبدون أيقونة قلب**.
- مسافات أوسع بين الأعمدة وخلفية رمادية فاتحة للمخطط.
- تمييز البطاقة المحددة بحلقة زرقاء.
- الإبقاء على إطار ذهبي للمتزوج وإطار أسود للمتوفى كمؤشرات حالة.

### لوحة التفاصيل

- عند الضغط على شخص تفتح **لوحة جانبية (Sheet)** بدل نافذة وسطية فقط — أقرب لتجربة مواقع النسب المحترفة.
- قسم **فصل الربط** يبقى واضحاً داخل اللوحة.

### الملفات

- `app/src/components/tree/FamilyChart.tsx`
- `app/src/components/tree/ChartConnectors.tsx`
- `app/src/pages/TreeWorkspace.tsx`

---

## 2026-07-26 — استقرار المخطط، فصل الروابط، وطباعة الشمس

### فصل الربط دون حذف الشخص

- في بطاقة تفاصيل الشخص (الضغط على البطاقة في المخطط أو القائمة) يظهر قسم **الروابط العائلية**.
- زر واضح **فصل الربط** بجانب:
  - الأب / الأم
  - الزوج أو الزوجة
  - الأبناء
- العملية تحذف علاقة القرابة فقط؛ الشخص يبقى في الشجرة ويمكن ربطه لاحقاً.
- API: `person.removeRelationship` (يتطلب دور محرر أو أعلى، والشجرة نشطة).
- الملفات: `app/src/pages/TreeWorkspace.tsx`، `app/api/personRouter.ts`، ترجمات `detail.*` في `ar.ts` / `en.ts`.

### خطوط المخطط المتصلة (أبناء وأزواج)

- خطوط زوجية متصلة (`CoupleLink`) من حافة لحاقة مع أيقونة القلب فوق الخط.
- تفرّع الأبناء (`SiblingFork` / `BranchRow`): جذع واحد + شريط أفقي متصل + فروع رأسية دون فجوات.
- الملفات: `app/src/components/tree/ChartConnectors.tsx`، `FamilyChart.tsx`.

### محاذاة خط الإخوة (مثال: حمود ↔ خميس)

- المشكلة: عمود الأخ ذي العائلة الواسعة (زوجات + أحفاد) كان يوسّع العمود، فيسقط خط الإخوة في منتصف العرض الكامل بعيداً عن البطاقة.
- الحل:
  - صف الزوجية متمركز على الشخص المحوري عبر شبكة `1fr / auto / 1fr` (`SiblingHub`).
  - الأبناء يأخذون عرضاً حقيقياً حتى لا تتداخل الفروع.
- الملف: `app/src/components/tree/FamilyChart.tsx`.

### حدود حالة البطاقة

- إطار ذهبي للمتزوج.
- إطار أسود للمتوفى.
- استقرار المخطط عند الإضافة/التعديل (بدون إعادة تركيب كاملة تفقد التكبير والسحب).
- ربط نسب أكثر أماناً (بدون دمج تلقائي غامض للآباء بالاسم).

### قالب طباعة الشمس (MyHeritage-style)

- المشكلة: أسماء نسب طويلة + زوايا ضيقة → تداخل كثيف في الوسط والحلقات.
- الحل:
  - توزيع زاوي حسب وزن الفرع (subtree weight).
  - فرض مسافة دنيا بين العقد على كل حلقة.
  - اختصار أسماء الحلقات الخارجية (الاسم فقط أو مع الأب باختصار).
  - تظليل خفيف حول النص + تناوب بسيط لإزاحة التسمية.
- الملفات: `app/src/lib/printData.ts` (`computeSunLayout`)، `app/src/components/print/SunRingPrint.tsx`، اختبارات `printData.test.ts`.

### تنظيف المستودع

- تجاهل `node_modules/` في جذر المشروع عبر `.gitignore`.

### commits ذات الصلة (الأحدث أولاً)

| Commit | الملخص |
|--------|--------|
| `f8bb6d6` | إصلاح تداخل أسماء قالب الشمس |
| `2ef8346` | أزرار فصل الربط واضحة (أب/زوج/أبناء) |
| `7c19f9c` | منع تداخل فروع الأبناء مع بقاء خط الإخوة على البطاقة |
| `a317310` | محاذاة خط الإخوة للشخص المحوري |
| `8408218` | تجاهل root `node_modules` |
| `851b263` | خطوط موصلات متصلة للأزواج والأبناء |
| `2f17cc6` | استقرار المخطط + ربط آمن + حدود الحالة |
| `f8e8d38` | جذر أساسي واحد + إبقاء الزوجات مع الأزواج |

---

## قوالب الطباعة المتاحة

المسار: `/trees/:id/print` — 13 قالباً:

| المعرّف | الوصف المختصر |
|---------|----------------|
| `palm` | نخلة عُمانية |
| `manuscript` | مخطوط |
| `book` | كتاب |
| `poster` | ملصق كبير |
| `map` | خريطة أماكن |
| `clan` | فخذ / قبيلة |
| `occasions` | مناسبات |
| `ornate` | مزخرف |
| `fan` | مروحة أجيال |
| `sun` | شمس نسب دائرية |
| `classic` | مخطط كلاسيكي |
| `pedigree` | نسب أعمدة |
| `heritage` | لوحة تراث |

نطاق الطباعة قابل للتحديد: فرع، جذر، عدد الأجيال، عرض الإناث، تضمين الآباء/الأزواج.

---

## كيفية الاستخدام السريع

### فصل ربط خاطئ

1. افتح الشجرة → تبويب **المخطط** أو **القائمة**.
2. اضغط على الشخص.
3. في قسم **الروابط العائلية** اضغط **فصل الربط** بجانب العلاقة الخاطئة.
4. أكّد — الشخص لا يُحذف.

### طباعة أوضح

1. `/trees/:id/print` → اختر القالب.
2. من لوحة النطاق قلّل الأجيال أو اختر فرعاً إذا كانت الشجرة كبيرة.
3. لقالب الشمس: التسميات في الحلقات الخارجية مختصرة عمداً لقابلية القراءة.

---

## ملاحظات تقنية للمطورين

```bash
cd app
npm run check          # TypeScript
npm test               # vitest (يشمل printData)
npm run dev            # http://localhost:5173
```

ملفات أساسية للمخطط والطباعة:

- `app/src/components/tree/FamilyChart.tsx`
- `app/src/components/tree/ChartConnectors.tsx`
- `app/src/pages/TreeWorkspace.tsx`
- `app/src/lib/printData.ts`
- `app/src/components/print/*`
